const
  express = require('express'),
  router = express.Router({ mergeParams: true }),
  {
    noRoutesLoaded
  } = require('../../../controllers/api/miscAction.controller');

router
  .route('/')
  .get(noRoutesLoaded);

module.exports = router;